---
// EJEMPLO: Cómo proteger una ruta con autenticación
// Copia este código en cualquier página .astro que quieras proteger

import BaseLayout from '../layouts/BaseLayout.astro';
import Navbar from '../components/Navbar';
import { verificarAutenticacion } from '../lib/auth-helper';

// Verificar autenticación - Redirige a /login si no está autenticado
const redirectResponse = await verificarAutenticacion(Astro);
if (redirectResponse) {
  return redirectResponse;
}

// Si llegamos aquí, el usuario está autenticado ✅
---

<BaseLayout title="Página Protegida">
  <Navbar client:load />
  
  <div class="pt-24 pb-12 px-6">
    <div class="max-w-4xl mx-auto">
      <h1 class="text-4xl font-bold text-white mb-4">
        Esta es una página protegida 🔐
      </h1>
      <p class="text-dark-500">
        Solo usuarios autenticados pueden ver este contenido.
      </p>
    </div>
  </div>
</BaseLayout>
